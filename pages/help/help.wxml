<view class="page-container">

  <view class="help-header">
    <text class="hh-title">{{isMentor ? '导师指引' : '使用指南'}}</text>
    <text class="hh-sub">欢迎使用「师傅在吗」{{isMentor ? '银龄导师端' : ''}}</text>
  </view>

  <view class="guide-list">
    <view class="biz-card guide-item" wx:for="{{isMentor ? mentorGuides : studentGuides}}" wx:key="title">
      <view class="gi-row">
        <view class="text-badge gi-icon {{item.theme}}">{{item.icon}}</view>
        <view class="gi-body">
          <text class="gi-title">{{item.title}}</text>
          <text class="gi-desc">{{item.desc}}</text>
        </view>
      </view>
    </view>
  </view>

  <view class="help-tips">
    <view class="section-title">常见问题</view>
    <view class="biz-card tip-item">
      <text class="tip-q">如何修改个人资料？</text>
      <text class="tip-a">在"我的"页面点击"编辑"按钮，可修改姓名、学科、教龄等信息。</text>
    </view>
    <view class="biz-card tip-item" wx:if="{{isMentor}}">
      <text class="tip-q">如何设置排班时间？</text>
      <text class="tip-a">在"我的"→"排班设置"中，选择每周可接单的时间段，系统将仅在该时段为您推送订单。</text>
    </view>
    <view class="biz-card tip-item" wx:if="{{!isMentor}}">
      <text class="tip-q">怎样切换到导师身份？</text>
      <text class="tip-a">退出当前账号后，在登录页选择"银龄导师"身份重新登录即可。</text>
    </view>
    <view class="biz-card tip-item">
      <text class="tip-q">提现需要多久到账？</text>
      <text class="tip-a">提现申请提交后，将在 1-3 个工作日内到账至您的微信钱包。</text>
    </view>
  </view>
</view>
